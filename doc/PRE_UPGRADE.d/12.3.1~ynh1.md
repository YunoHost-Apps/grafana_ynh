That new version:

- does not rely anymore to the debian package provided by the grafana repository
- does not require/depend on the influxdb package (v1), if you need it, you should explicity install it by cli: `apt install influxdb`
